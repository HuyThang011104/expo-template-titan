# NOTE: pnpm-specific settings (e.g. virtualStoreDirMaxLength) do NOT belong here.
# Since pnpm 11 they are only read from YAML files, so the virtual-store pin
# lives in pnpm-workspace.yaml. A copy here would be silently ignored by pnpm
# and only trigger "Unknown project config" warnings under npm.
