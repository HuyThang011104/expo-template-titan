// Thin route: re-exports the feature screen.
// Dev-only template showcase — no tab/stack links, open via deep link when logged in.
// No fetch hooks, no atomic UI here.
export { ShowcaseScreen as default } from "@/features/showcase";
