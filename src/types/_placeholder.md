# `src/types` — placeholder (Phase 3)

Global shared types that belong to no entity (e.g. branded IDs, deep-link payload).
Prefer colocated types in `features` / `entities` / `shared`; only move them here when truly global.
