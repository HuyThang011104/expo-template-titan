// Public API of `src/shared/auth`. Storage state stays internal to the provider.
export { SESSION_KEY, SessionProvider, type SessionContextValue } from "./session-provider";
export { useSession } from "./use-session";
